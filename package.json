{
  "name": "dsd-cohort-2024",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently npm:dev-client npm:dev-server",
    "dev-client": "cd client/ && npm install && npm run dev",
    "dev-server": "cd server/ && gradlew build && gradlew run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}